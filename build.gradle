// Root build.gradle (placeholder)
buildscript {
    repositories { mavenCentral() }
    dependencies { classpath "com.android.tools.build:gradle:7.0.4" }
}
allprojects { repositories { mavenCentral() } }
